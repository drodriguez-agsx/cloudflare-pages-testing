# Dependencies (if/when build tooling is added)
node_modules/

# Secrets & environment
.env
.env.*
!.env.example

# Cloudflare Wrangler local cache/state
.wrangler/

# NOTE: dist/ is intentionally COMMITTED — it's the deployable staging gallery
# + per-tenant bundles the team browses (see README "Staging preview").
# Regenerate it with `npm run build`; don't hand-edit files under dist/.

# OS / logs
.DS_Store
*.log

.dev.vars*
!.dev.vars.example
